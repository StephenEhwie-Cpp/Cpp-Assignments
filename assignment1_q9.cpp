#include <iostream>
using namespace std;

int main() {
    int x = 5;

    
    if (x == 5) {
        cout << "x is equal to 5 (using ==)" << endl;
    }

    cout << "\nExplanation:" << endl;
    cout << "=  is the ASSIGNMENT operator – it assigns a value to a variable." << endl;
    cout << "== is the EQUALITY operator – it compares two values." << endl;

    return 0;
}